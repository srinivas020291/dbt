version: 2

sources:
  - name: hr
    database: DBT
    schema: hr
    tables: 
      - name: employees1
        loaded_at_field: load_time
        freshness: 
          warn_after: {count: 12, period: hour}
          error_after: { count: 24, period: hour}
  - name: sse_dm_mkt_prd_dm
    database: DBT
    schema: sse_dm_mkt_prd_dm
    tables: 
      - name: MDW_CAMPAIGNS
        description: "Marketing campaigns"
        
  - name: EMP
    database: DBT
    schema: EMP
    tables:
      - name: employees
        loaded_at_field: load_time
        freshness: 
          error_after:
            count: 2
            period: day
          warn_after:
            count: 1
            period: day
        
      - name: department
          
